import { Typography } from "@mui/material"


const Container = () => {

    return (
        <section className="text-white">
            <Typography
                component="h1"
                className="text-lg">
                Hi there!
            </Typography>
        </section>
    );
};

export default Container;